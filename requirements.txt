# SciML-MD dependencies.
#
# ONE file. The README used to send people to requirements_phase1/2/3.txt,
# which have never existed in this repository (OI-46) - a reader following the
# install instructions failed on the first command.
#
# pyemma was pinned here until 2026-09-12 and is now removed: nothing in the
# codebase imports it, it is deprecated upstream, and it fights arm64 installs.
# The MSM machinery is deeptime throughout.

# Core scientific computing
numpy>=1.20.0
scipy>=1.7.0
pandas>=1.3.0
scikit-learn>=1.0.0

# MSM estimation (tICA, clustering, reversible MLE, CK tests)
deeptime>=0.4.0

# MD trajectory I/O and geometry
mdtraj>=1.9.0

# Data formats
h5py>=3.0.0          # mdCATH per-domain archives
pyarrow>=6.0.0       # parquet, used by msm/bootstrap_msm.py and scoring/anomaly_v2.py
openpyxl>=3.0.0      # validation tracker
pyyaml>=5.4.0

# Optional: hidden-Markov soft states (msm/soft_states.py only)
hmmlearn>=0.2.7

# Tests
pytest>=7.0.0
